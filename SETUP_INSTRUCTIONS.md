We already had things which concern with courses like courses_module : course_content: reading as pdf and documents with local storage, and also videos as mp4 also urls link as local storage. But we still have to implement the quiz which included in course_content as content_type ENUM. You have to read the database/lms_schema.sql, you will understand about the entities and attributes of Quiz and course. Instructor can create all the content type, right now we can create reading, video so need to do quiz. Please do which is concerned with quiz and I just need backend api for testing only. How will request the quiz as a google form link or mcq type, do what is most appropriate one for our project. 